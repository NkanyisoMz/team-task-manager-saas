<h1><%= @project.name %> Tasks</h1>

<%= link_to "New Task", new_team_project_task_path(@team, @project) %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, team_project_task_path(@team, @project, task) %>
      (<%= task.status %>)
    </li>
  <% end %>
</ul>